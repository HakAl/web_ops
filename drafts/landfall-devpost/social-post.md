# Optional social post drafts

Status: editable drafts. Do not publish.

## Short version

We built Landfall for the #AllThingsAgenticHackathon: one bounded approval starts a defect-triage agent fleet, workers act inside fenced authority, and a deterministic verifier controls terminal state. The demo keeps its bounded real-cloud evidence separate from its deterministic local governance harness. [PUBLIC_DEVPOST_URL]

## LinkedIn version

Landfall is our Fortified Enterprise Fleet entry for the #AllThingsAgenticHackathon.

A support lead approves one bounded repair envelope, then steps away. A triage agent structures the redacted typed failure, a supervisor delegates work, workers act inside fenced attempts, and a deterministic verifier alone controls terminal state. The worker cannot write terminal state.

Our demonstration keeps its evidence boundary visible. One beat shows the bounded deployed slice. The kill, lease-expiry, successor-lineage, stale-closeout, and structural-verifier sequence is a deterministic local governance demonstration, and it does not establish that the defect is fixed.

Project: [PUBLIC_DEVPOST_URL]
Code: [PUBLIC_GITHUB_REPOSITORY_URL]

## Publication gate

- Replace public URL placeholders.
- Publish only after the repository, Devpost page, and video are public.
- Keep `#AllThingsAgenticHackathon` exactly as written.
- Do not add a claim that the complete fleet or kill sequence ran on Google Cloud.
