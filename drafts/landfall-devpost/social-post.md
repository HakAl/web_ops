# Optional social post drafts

Status: editable drafts. Do not publish.

## Short version

We built Landfall for the #AllThingsAgenticHackathon: one bounded approval starts a defect-triage agent fleet, workers act inside fenced authority, and a deterministic verifier decides completion. The demo separates the real Cloud Run and Gemini probe from the local kill and succession harness. [PUBLIC_DEVPOST_URL]

## LinkedIn version

Landfall is our Fortified Enterprise Fleet entry for the #AllThingsAgenticHackathon.

A support lead approves one bounded repair envelope, then steps away. Gemini triage structures the failure, a supervisor delegates work, workers act inside fenced attempts, and a deterministic verifier retrieves or reproduces the evidence that decides completion. The worker cannot write terminal state.

Our demonstration keeps its evidence boundary visible. The Cloud Run, ADK, and Gemini probe is real Google Cloud evidence. The kill, lease-expiry, successor-lineage, stale-closeout, and verifier sequence is a deterministic local governance demonstration.

Project: [PUBLIC_DEVPOST_URL]
Code: [PUBLIC_GITHUB_REPOSITORY_URL]

## Publication gate

- Replace public URL placeholders.
- Publish only after the repository, Devpost page, and video are public.
- Keep `#AllThingsAgenticHackathon` exactly as written.
- Do not add a claim that the complete fleet or kill sequence ran on Google Cloud.
